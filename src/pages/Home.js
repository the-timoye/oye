import React from 'react';
import { Nav, Header, SectionTitle } from "../components";

export const Home = () => {
    return (
        <>
            <Nav />
            <Header />
            <SectionTitle title={"My Skills"} />
        </>
    )
}